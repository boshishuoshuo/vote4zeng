import 'package:flutter/material.dart';

class DonationAndVolunteerScreen extends StatelessWidget {
  const DonationAndVolunteerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
